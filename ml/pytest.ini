[pytest]
asyncio_mode = auto
markers =
    e2e: marks tests as real LLM end-to-end tests (may be slow / cost $; skipped unless RUN_E2E=1)
    fullstack: marks tests that require the full backend+ml+db stack
