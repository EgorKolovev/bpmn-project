-r requirements.txt

# Test stack
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
pytest-rerunfailures==15.1

# Sync Postgres driver — used by tests/test_migrations.py for the
# `alembic upgrade head` round-trip and the `inspect()` schema probe.
# Not shipped in the runtime image.
psycopg2-binary==2.9.10

# Lint / format
ruff==0.8.4
pre-commit==4.0.1
